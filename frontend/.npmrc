engine-strict=true
ignore-scripts=true

; Block newly published packages (3-day cooldown, matching bunfig.toml)
; npm has no native minimumReleaseAge — this comment documents intent
; Enforced by bunfig.toml for bun, and by CI policy for npm
