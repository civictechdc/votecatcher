# CI/test placeholder values.
# Safe to check in — contains no real secrets.
# Used by `vite build --mode test` and vitest (test mode).

PUBLIC_API_URL=http://localhost:8080
PUBLIC_DEMO_MODE=false
DEMO_MODE=false
ORIGIN=http://localhost:5173
DATABASE_URL=postgresql://ci:ci@localhost:5432/ci  # pragma: allowlist secret
BETTER_AUTH_SECRET=ci-placeholder-secret  # pragma: allowlist secret
OCR_PROVIDER_NAME=openai
OCR_PROVIDER_MODEL=gpt-4o-mini
OCR_PROVIDER_API_KEY=ci-placeholder  # pragma: allowlist secret
