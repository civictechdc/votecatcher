# These are false positives from the python:3.13-slim base image SBOM.
# The fixed versions (msgpack 1.2.1, setuptools 84.0.0) are confirmed
# installed in the Docker image filesystem, but Trivy reads the stale
# SBOM attestation from the base image layer which still references
# the old vulnerable versions.
GHSA-6v7p-g79w-8964
CVE-2025-47273
# libcap2 CVE-2026-4878: TOCTOU race in cap_set_file().
# Debian OS package in the frontend base image. No fixed base image
# available yet (fixed in 1:2.75-10+deb13u1, not yet in stable).
CVE-2026-4878
# util-linux CVE-2026-53615: Integer overflow in libblkid/src/partitions/dos.c.
# Affects bsdutils, libblkid1, libmount1, libuuid1, libsmartcols1,
# liblastlog2-2, mount, util-linux in the frontend base image
# (oven/bun:1.3.14, Debian 13/trixie). Fixed in 2.41.5-0+deb13u1,
# not yet available in stable base image.
CVE-2026-53615
