**REMinders for developers:**

1. **`.agent-workspace/` - Local development workspace (notes, design sessions, implementation plans)
2. **`.agent/` - Agent configuration and skills (opencode skills)
3. **Don't commit these files or directories to source control**

4. **Use `.gitignore** to ignore them

4. **Never commit directly to `.agent/` or with `.agent-workspace/` - they like ** files should be preserved locally
5. **Documentation** - Keep docs updated, remove stale docs

 update PROGRESS.md with progress
6. **Review pull requests** - Continue Phase 3 work

 but commits should be grouped logically