# dependencies
node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# production
/build
dist

# SvelteKit
.svelte-kit/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
bun-debug.log*

# local env files
.env*.local
.env.local
.env.development.local
.env.test.local
.env.production.local
!.env.local.example

# typescript
*.tsbuildinfo

# Supabase
.supabase

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
Thumbs.db

# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# npm cache
.npm

# eslint cache
.eslintcache

# Output of 'npm pack'
*.tgz

# parcel-bundler cache
.cache
.parcel-cache

# Temporary folders
tmp/
temp/

# Plans (local development only)
plans/

# Agent workspace — blocked by pre-commit hook (reject-internal-dirs.sh), not gitignored
# .agent-workspace/ files are tracked so samples/artifacts survive across sessions
.agent/






# Secret detection baseline (contains detected patterns)
.secrets.baseline

# Cursor
.cursor/

# Environment files (catch-all)
.env
.env.*
!.env.example

# API keys and secrets
*.key
*.crt
*.p12
*.pfx

# Database files
*.db
*.sqlite
*.sqlite3

# Backup files
*.bak
*.backup
*~

# OS generated files
.DS_Store?
ehthumbs.db
Icon?

# Supabase specific
supabase/.temp/
supabase/.env

# AI/ML model files
*.model
*.bin
*.onnx
*.tflite

# Large data files
*.csv
*.json
!package.json
!tsconfig.json
!eslint.config.js
!.github/renovate.json
!.zed/settings.json
!.zed/debug.json
!.zed/tasks.json
!.vscode/settings.json
!.devcontainer/devcontainer.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
!backend/typecheck-baseline.json
!backend/pyrightconfig.json
!backend/tests/**/assets/*.json
!frontend/.fallowrc.json

# Upload directories
uploads/
temp-uploads/

# Cursor
.cursor/

# Votecatcher data files (contain sensitive batch/API data)
Votecatcher/

# data files
sample_data
__pycache__
.venv
*.egg-info
*.jsonl

# .claude/ — kept in .gitignore for clean 'git status' output.
# Pre-commit hook (.gitblock) blocks .claude/ entirely — these entries
# just prevent untracked-file noise in status/diff.
.claude/hooks/
.claude/commands/
.claude/mcp/

# .claude/ — DO track these (if manually configured):
#   .claude/settings.json         — Claude Code project settings
#   .claude/settings.local.json is per-developer, ignore separately if needed
